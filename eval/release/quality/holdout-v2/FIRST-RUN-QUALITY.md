# Holdout-v2 首次真实质量审核：未通过

2026-09-16，独立评审在 12 条质量请求全部结束后才读取金标，逐项核对已显示正文、数值和口径、原页及逐字引用，并以只读数据库导出核对发布与复用事实。此处保留首次结果；后续修复不得覆盖它。机器可读证据与冻结版本摘要见 [first-run-quality-result.json](first-run-quality-result.json)。

| 类别 | 通过 / 总数 | 判定 |
| --- | --- | --- |
| 有充分证据的正例 | 5 / 6 | **未通过** |
| 缺证据负例 | 2 / 2 | 通过 |
| 不支持口径负例 | 2 / 2 | 通过 |
| 已发布事实复用 | 2 / 2 | 通过，均为 FULL 且零增量模型请求 |

`q-hengli-net-profit` 未通过。恒立液压原年报物理第 68 页的合并利润表，明确列出 2024 年、人民币元及完整净利润行 **2,512,380,605.82**；实际回答为 `INCONCLUSIVE`，正文为“证据不足，未给出确定数值”，验证原因是 `ROW_OUTSIDE_HEADER_TABLE`。可信表格中的单元格内换行导致验证器误判表格结束。这是对可回答问题的错误弃答，不能因没有编造数值而计为正例通过。

其余已给出的确定性数值、实体、年度、人民币元单位及合并口径均与原页和金标一致。评审未发现错误确定性声明。四个负例均明确弃答或说明不支持，所属租户的正式事实和抽取批次均为零；没有把母公司口径替代合并口径。

另行核对了两家公司、两种实验臂的 **16 条成本序列答案**：16 / 16 的显示正文、值、引用与原页均正确。构建臂重复提问的四条答案均正确、FULL 且零模型请求。因此可以比较**这 16 条已核对查询的累计模型费用**，包括前期构建开销；这不构成总体质量等价或普遍降本结论，也不改变上面的质量门槛失败。

数据库复核覆盖这 28 条请求及其关联的 14 个独立租户组、6 条正式事实、6 份验证报告和 6 条复用记录。已核对所有发布事实及复用记录的值、口径、租户、文档版本、报告和来源。原 PDF、选页 PDF、逐页内容流及渲染图的哈希与冻结准备记录相符；原页 80/81（浙江龙盛）和 68/69（恒立液压）均经人工目视核验。私有原始结果及只读审计导出保留在执行方的私有证据目录；公开报告不包含 Run、attempt、tenant 标识或密钥。

本轮只覆盖两个有意选择的同页文本型财报来源；未覆盖扫描件、OCR、跨页复杂表格或任意财务问题。由于失败已经用于定位修复，整个 holdout-v2 此后归为回归集。有限质量门槛仍为 **FAIL**，修复后的独立验收应使用重新冻结的新来源，不能把本集重跑当作新的独立通过结果。
